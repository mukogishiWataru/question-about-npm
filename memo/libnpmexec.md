npm exec (npx)コマンドを叩くと実行されるプログラム。<br>
The npm exec (npx) Programmatic API

- run()
```
// run-script.js
  const run = () => runScript({
    args,
    call,
    flatOptions,
    locationMsg,
    path,
    binPaths,
    runPath,
    scriptShell,
  })
```